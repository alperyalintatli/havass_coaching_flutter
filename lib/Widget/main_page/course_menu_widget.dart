import 'package:flutter/material.dart';

class CourseWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Course widget"),
    );
  }
}
